import { describe, it, expect } from 'vitest';
import { RatingStars } from '../components/common/RatingStars';

describe('RatingStars Component', () => {
  it('renders rating correctly', () => {
    expect(RatingStars).toBeDefined();
  });
});
