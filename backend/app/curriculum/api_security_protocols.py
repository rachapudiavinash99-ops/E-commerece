"""
Module: Enterprise API Security, OAuth2, OpenID Connect, and Cryptography Protocols
"""

from typing import List, Dict, Any

API_SECURITY_PROTOCOLS: List[Dict[str, Any]] = [
    {
        "id": 1,
        "protocol_name": "Security Protocol #1: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 2,
        "protocol_name": "Security Protocol #2: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 3,
        "protocol_name": "Security Protocol #3: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 4,
        "protocol_name": "Security Protocol #4: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 5,
        "protocol_name": "Security Protocol #5: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 6,
        "protocol_name": "Security Protocol #6: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 7,
        "protocol_name": "Security Protocol #7: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 8,
        "protocol_name": "Security Protocol #8: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 9,
        "protocol_name": "Security Protocol #9: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 10,
        "protocol_name": "Security Protocol #10: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 11,
        "protocol_name": "Security Protocol #11: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 12,
        "protocol_name": "Security Protocol #12: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 13,
        "protocol_name": "Security Protocol #13: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 14,
        "protocol_name": "Security Protocol #14: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
    {
        "id": 15,
        "protocol_name": "Security Protocol #15: Cryptographic Standard",
        "specification": """
        - RFC 7519 JSON Web Token (JWT) signature validation
        - PKCE (Proof Key for Code Exchange) OAuth2 Authorization Flow
        - AES-256-GCM symmetric payload encryption for sensitive PII
        - Ed25519 / RSA-4096 asymmetric digital signing for certificates
        - CSP (Content Security Policy) and CORS preflight protection
        - CSRF token synchronization with SameSite cookies
        """
    },
]
